# 安裝 React Native Skia

為了實現高質感的 2.5D Q 版果凍風格 UI，需要安裝 Skia。

## ✅ 當前狀態

- ✅ `react-native-reanimated` 已安裝（v4.2.1）
- ⚠️ `@shopify/react-native-skia` 需要手動安裝

## 安裝步驟

### 1. 安裝 Skia

```bash
npm install @shopify/react-native-skia
```

### 2. 重新構建原生項目

```bash
# 清理並重新生成原生代碼
npx expo prebuild --clean

# 運行 iOS（或 Android）
npx expo run:ios
# 或
npx expo run:android
```

## ⚠️ 重要注意事項

1. **Skia 需要原生編譯**：無法在 Expo Go 中運行
2. **必須使用 Development Build**：需要 `npx expo run:ios` 或 `npx expo run:android`
3. **安裝後需要重新構建**：不能直接熱重載

## 🔄 自動降級機制

組件已實現**智能檢測**：
- ✅ 如果 Skia 已安裝 → 自動使用 Skia 渲染（最佳視覺效果）
- ⚠️ 如果 Skia 未安裝 → 自動降級使用 SVG（已安裝，可正常運行）

## 📝 驗證安裝

安裝後，查看控制台日誌：
- ✅ `[StatusCapsule] ✅ Skia loaded successfully` → Skia 已成功載入
- ⚠️ `[StatusCapsule] ⚠️ Skia not installed, using SVG fallback` → 使用 SVG 備用方案

## 🎨 視覺差異

- **Skia 版本**：更流暢的動畫、更精緻的光澤效果、GPU 加速渲染
- **SVG 版本**：功能完整，但視覺效果略遜於 Skia
